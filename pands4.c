/* pands4.c */
#include <stdio.h>
int main()
{
    int minx, x, y, z;
    printf("Enter four ints: ");
    scanf( "%d %d %d %d", &minx, &x, &y, &z);
    printf("You wrote: %d %d %d %d", minx, x, y, z);
    return 0;
}
